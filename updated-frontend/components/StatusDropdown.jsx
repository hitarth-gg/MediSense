"use client"

// This component is no longer needed as facilitators cannot change patient status
// Status changes are now handled by doctors only
export default function StatusDropdown() {
  return null
}
